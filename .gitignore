node_modules/
dist/
.astro/
.env
.env.production
.DS_Store
.vercel/
npm-debug.log*

# Source photography — 10MB+ each. The optimised, cropped versions
# that the site actually serves live in public/me/. Keep the
# originals locally; don't carry them in git forever.
assets/me/*.png

# Local transfer folder — never part of the site.
_zip/
