---
import Desktop from '../layouts/Desktop.astro';
import { folders } from '../data/folders';

export function getStaticPaths() {
  return folders.map((f) => ({ params: { folder: f.id } }));
}

const { folder } = Astro.params;
---

<Desktop active={folder as never} />
