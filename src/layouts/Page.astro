---
import Base from './Base.astro';
import Sky from '../components/Sky.astro';
import Taskbar from '../components/Taskbar.astro';
import FolderIcons from '../components/FolderIcons.astro';
import GlassWin from '../components/GlassWin.astro';
import '../styles/glass.css';
import { getFolder, type FolderId } from '../data/folders';

interface Props {
  active: FolderId;
  lead: string;
}

const { active, lead } = Astro.props;
const folder = getFolder(active)!;
---

<Base title={folder.label} description={lead} fonts>
  <!-- The desktop is identical on every page. Only the window
       on top of it changes. -->
  <Sky />

  <div class="workspace">
    <div class="rail">
      <FolderIcons active={active} />
    </div>

    <main class="stage g" id="main">
      <GlassWin title={folder.label}>
        <header class="lead">
          <p class="label">{folder.question}</p>
          <h1>{folder.label}</h1>
          <p class="sub">{lead}</p>
        </header>

        <slot />
      </GlassWin>
    </main>
  </div>

  <Taskbar active={active} />
</Base>

<style>
  .workspace {
    position: relative;
    z-index: 10;
    display: flex;
    align-items: flex-start;
    gap: 28px;
    min-height: calc(100vh - var(--taskbar-h));
    padding: 22px;
  }

  /* The folders never move. They sit on the desktop, and the
     window scrolls past them. */
  .rail {
    position: sticky;
    top: 22px;
    align-self: flex-start;
    flex: none;
  }

  .stage {
    flex: 1;
    min-width: 0;
    max-width: 1080px;
    padding-bottom: 34px;
  }

  .lead {
    max-width: 66ch;
    padding-bottom: 34px;
    margin-bottom: 34px;
    border-bottom: 1px solid var(--g-border-soft);
    box-shadow: 0 1px 0 rgba(255, 255, 255, 0.6);
  }

  .lead .label {
    display: block;
    margin-bottom: 14px;
  }

  .lead h1 {
    margin-bottom: 18px;
  }

  .sub {
    font-size: 16px;
    font-weight: 300;
    line-height: 1.62;
    color: var(--g-text-2);
  }

  @media (max-width: 900px) {
    .workspace {
      flex-direction: column;
      gap: 20px;
      padding: 16px 14px;
    }

    .rail {
      position: static;
      width: 100%;
    }

    .stage {
      width: 100%;
      max-width: none;
    }

    .sub {
      font-size: 15px;
    }
  }
</style>
